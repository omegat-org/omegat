steps:
  - task: Cache@2
    displayName: 'Cache Gradle'
    condition: ${{ parameters.condition }}
    inputs:
      key: 'gradle-docs | "$(Agent.OS)" | gradle/wrapper/gradle-wrapper.properties | build-logic/build.gradle | build-logic/src/main/groovy/org.omegat.document-conventions.gradle | src/docs/manual | src/docs/xsl | src/docs/style'
      restoreKeys: |
        gradle-docs | "$(Agent.OS)"
        gradle | "$(Agent.OS)"
        gradle
      path: '$(GRADLE_USER_HOME)/caches/modules-2'
  - script: python3 ci/check-docs-assets.py
    condition: ${{ parameters.condition }}
    displayName: 'Check documentation assets are referenced'
  - task: Gradle@3
    condition: ${{ parameters.condition }}
    inputs:
      tasks: 'manualHtmls'
      options: '-PenvIsCi --no-daemon'
      jdkVersionOption: '1.21'
    displayName: 'Run Gradle clean&webManual'
