parameters:
  - name: omegatVersion
    default: ''

steps:
  - task: DownloadPipelineArtifact@2
    displayName: 'Download Artifact: drop'
    inputs:
      targetPath: $(Pipeline.Workspace)/drop
  - task: Bash@3
    displayName: 📂 SFTP upload to sourceforge file management
    inputs:
      targetType: 'inline'
      script: |
        echo "put $(Pipeline.Workspace)/drop/* /home/frs/project/omegat/OmegaT\\ -\\ Latest/OmegaT\\ ${{ parameters.omegatVersion }}" > sftp.cmd
        SSHPASS=$(SOURCEFORGE_FILE_PASS) sshpass -e sftp -oBatchMode=no -b sftp.cmd -oStrictHostKeyChecking=no $(SOURCEFORGE_FILE_USER)@frs.sourceforge.net
