<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<section id="menus.goto">
  <title>转到</title>

  <para>仅当打开了项目后此菜单下才会显示条目。</para>

  <variablelist>
    <varlistentry>
      <term>下一个未译片段</term>

      <listitem>
        <para>移动到下一个在翻译记忆库中无相同内容的片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>下一个已译片段</term>

      <listitem>
        <para>移动到下一个已译片段，忽略未译片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>下一个片段</term>

      <listitem>
        <para>移动到下一个片段。如果当前片段是文件中的最后一个片段，则会移动到下一个文件的第一个片段。</para>

        <para>注意：如果未选择<link linkend="dialogs.preferences.general.usetabtoadvance">用 TAB 前进</link>选项，则亦可用 <keycap>Enter</keycap> 快捷键。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>上一个片段</term>

      <listitem>
        <para>移动到上一个片段。如果当前片段是文件中的第一个片段，则会移动到上一个文件的最后一个片段。</para>

        <para>注意：如果未选择<link linkend="dialogs.preferences.general.usetabtoadvance">用 TAB 前进</link>选项，则亦可用<keycombo> <keycap>Ctrl</keycap> <keycap>Enter</keycap> </keycombo>快捷键。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>片段序号...</term>

      <listitem>
        <para>在输入片段的序号或编号后就会打开该片段</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>下一个备注</term>

      <listitem>
        <para>会打开下一个带有备注的片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>上一个备注</term>

      <listitem>
        <para>会打开上一个带有备注的片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>所选匹配的来源</term>

      <listitem>
        <para>移动到<guilabel>模糊匹配</guilabel>窗格中当前选择的匹配相对应的片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>在历史中前进...</term>

      <listitem>
        <para><application>OmegaT</application> 会记住处理过的片段。使用此命令你可以前进到之前用<guimenuitem>在历史中后退</guimenuitem>...命令所离开的拿个片段。</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>在历史中后退...</term>

      <listitem>
        <para>使用此命令，你一次可以退后一个片段，稍后可以用下面的<guimenuitem>在历史中前进...</guimenuitem>命令回到当前片段。</para>
      </listitem>
    </varlistentry>
  </variablelist>
</section>