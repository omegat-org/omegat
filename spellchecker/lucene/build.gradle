plugins {
    id 'org.omegat.module'
}

def providedCoreLibsDir = rootProject.property('providedCoreLibsDir')

dependencies {
    compileOnly(project(':'))
    if (file(providedCoreLibsDir).directory) {
        compileOnly fileTree(dir: providedCoreLibsDir, includes: ['**/jspecify-*.jar', '**/commons-io-*.jar', '**/lucene-*.jar'])
    } else {
        compileOnly(libs.commons.io)
        compileOnly(libs.bundles.lucene)
        compileOnly(libs.jspecify)
    }
    testImplementation(libs.junit4)
    testImplementation(libs.assertj.core)
    testImplementation(testFixtures(project(':')))
    testImplementation(libs.commons.io)
    testImplementation(libs.languagetool.de)
}

test {
    dependsOn project(':language-modules:de').tasks.withType(Jar)
    dependsOn project(':language-modules:fr').tasks.withType(Jar)
}
