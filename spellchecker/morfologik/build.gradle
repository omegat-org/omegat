plugins {
    id 'org.omegat.module'
}

def providedCoreLibsDir = omegatModule.providedCoreLibsDir

dependencies {
    compileOnly(project(':'))
    if (file(providedCoreLibsDir).directory) {
        compileOnly omegatModule.providedCoreLib('commons-io', 'languagetool-core', 'lib-mnemonics',
                'slf4j-format-jdk14', 'morfologik', 'jspecify')
    } else {
        // morfologik stemming and speller are dependency of languagetool-core
        compileOnly(libs.morfologik.stemming)
        compileOnly(libs.morfologik.speller)
        compileOnly(libs.commons.io)
        compileOnly(libs.omegat.mnemonics)
        compileOnly(libs.slf4j.format.jdk14)
        compileOnly(libs.languagetool.core)
        compileOnly(libs.jspecify)
    }
    testImplementation(libs.junit4)
    testImplementation(libs.assertj.core)
    testImplementation(testFixtures(project(':')))
    testImplementation(libs.commons.io)
    testRuntimeOnly(libs.languagetool.pl)
}

test {
    dependsOn project(':language-modules:pl').tasks.withType(Jar)
    dependsOn project(':language-modules:de').tasks.withType(Jar)
}
