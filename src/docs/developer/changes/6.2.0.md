# OmegaT 6.2.0 (dev)

Here is a changelog note for developers that has notable internal changes.
Architectural decisions are recorded in ADR -- please look at src/docs/developer/adr/

## Basic

- Reorganize the source tree structure as a Gradle/Maven standard
    - documents -> src/docs
    - core source/resources -> src/main
    - core tests -> src/test
    - test fixtures -> src/testFixtures
    - acceptance tests -> src/testAcceptance
- Build on JDK 21 and bundle JRE 25

## Build Tooling
- Migrate Java source/target compatibility to Gradle toolchain declaration
  (fixes daemon JVM mismatch on `invalid source release: 21`)
  [#1577](https://github.com/omegat-org/omegat/pull/1577)
- Enable configuration cache by default
- Signing native libraries(*.jnilib, *.dylib) of dependencies 
- `changedOnBranch` and `spotlessChangedApply` fail with the configuration cache; git remote detection is broken
  [BUGS#1801](https://sourceforge.net/p/omegat/bugs/1801/)
- SpotlessChangedApply task now supports the configuration cache
- Fix `initializeSonatypeStagingRepository` failure ("Failed to find staging
  profile for package group: ") by setting the root project's `group`, which
  was never assigned even though `mavenJava` publication set `groupId`
  explicitly. `nexusPublishing.packageGroup` derives from `project.group` by
  default, so it resolved to an empty string.-
- Fix hardcoded `groupId` and `version` (`'org.omegat'` / `'6.1.0'`) in the
  per-language manual `MavenPublication` definitions
  (`org.omegat.document-conventions.gradle`), which would otherwise continue
  publishing stale Maven coordinates after future releases. Now derived from
  `org.omegat.groupId` and `omtVersion.mavenStyleVersion`, matching the
  `mavenJava` publication.
- Fix syntax error in the postinst script for debian package [BUGS#1328](https://sourceforge.net/p/omegat/bugs/1328)/[PR#2243](https://github.com/omegat-org/omegat/pull/2243)
- The app title now shows commit sha + branch name for dev builds [PR#2222](https://github.com/omegat-org/omegat/pull/2222)
- Reduces distribution size by deduplication of shipped libraries [PR#2216](https://github.com/omegat-org/omegat/pull/2216)
- Refactor functions defined in `ext.` into native build libraries.[PR#2209](https://github.com/omegat-org/omegat/pull/2209)
- Integration test server Docker image base updated from `debian:bullseye` to
  `debian:bookworm`, following Debian 11 "bullseye" LTS end-of-life (2026-08-31).
  The `bullseye-security` package pool is no longer serving new fetches, causing
  `apt-get install` failures (404) in the server-side test container. Client-side
  image was already on `bookworm`; no functional changes to the SVN
  (`mod_dav_svn`/`mod_authz_svn`) or git-over-SSH test setup were required.

## Publish Tooling
- Extends Gradle JPackage tasks for MSI and PKG
- Publish CI/CD was fixed to use always "sftp://" [PR#2254](https://github.com/omegat-org/omegat/pull/2254)

## Utility Tooling
- Introduce `tool/` folder and provide several useful developer scripts
    - `check_duplicate_dependencies.sh` to detect duplicate dependencies in `build.gradle` files among core and modules
    - `git-code-analysis.sh` to show contribution stats.
    - `nullmarked_coverage.py` to show JSpecify's `@NullMarked` coverage stats against packages.
      - `props_key_lint.sh` to lint properties files for duplicate keys in the Bundle.properties.
- Moves quality assuarance test data into `tool/`
    - `qodana.sarif.json` is used to analysis with Qodana to detect delta. 

## Testing
- BundleTest with English locale
- Run integration test (docker) on Java 25 (PR#2131)

## Dependencies

Notable dependencies are

- Bump Gradle 9.7.1
- JSpecify 1.0.1
- JAXB XJC 4.0.9
- JGit 7.7.1.202607240634-r
- Jackson 2.22.1
- docbook-xslTNG 2.8.3

Please refer to the libs.versions.toml file for the full list of dependencies.

## API / SPI changes

- A method `IMainWindow#showStatusMessageRB(key, Objects.. args)` now accept a non-null key. When you want to clear the status bar, please pass an empty string.

## Internal

- Project main storage project_save.tmx is now in TMX 1.4 Level 2 [PR#1577](https://github.com/omegat-org/omegat/pull/1577)
- Install the preference store through a testable seam [PR#2110](https://github.com/omegat-org/omegat/pull/2110)
- Drop `org.omegat.core.teams2.impl.GITExternalGpgSigner` and JGit's native GPGSetup [PR#1946](https://github.com/omegat-org/omegat/pull/1946)
- migrate to LongProcessExecutor framework [PR#1905](https://github.com/omegat-org/omegat/pull/1905]
- migrate CLI command line parser with Picocli library [PR#1321](https://github.com/omegat-org/omegat/pull/1321)
- Encapsulate team operations for TMX and glossary as classes in org.omegat.core.team2.operation package [PR#1664](https://github.com/omegat-org/omegat/pull/1664)
- Add `package-info.java` around all the packages with `@NullMarked` and put `@Nullable` annotation on all the parameters and return values of public methods which are required. 
    (PR#2298, PR#2297, PR#2295, PR#2291, PR#2285, PR#2284, PR#2283, PR#2281, PR#2280, PR#2279, PR#2138)
- Editor colours table is reworked [PR#2189](https://github.com/omegat-org/omegat/pull/2189)
- chore: introduce thread-safe synchronization for core utilities and add nullability annotations (PR#2151)
- fix(filelist): release the closed project's file list (PR#2122)
- fix(gui): treat empty status msg key as null to avoid MissingResourceException (PR#2160)
- chore(core): replace `projectTMX` synchronization with `projectTMXLock` for thread safety (#2164)
- test(gui): decouple locale-dependent tests from the runtime locale (#2165)
- Team synchronization robustness improvements [PR#2214](https://github.com/omegat-org/omegat/pull/2214)
 