plugins {
    id 'org.omegat.module'
}

def providedCoreLibsDir = omegatModule.providedCoreLibsDir

dependencies {
    compileOnly(project(':'))
    if (file(providedCoreLibsDir).directory) {
        compileOnly omegatModule.providedCoreLib('jspecify')
        implementation omegatModule.providedModuleLib('flatlaf')
    } else {
        // platform independent dark theme
        implementation(libs.flatlaf)
        compileOnly(libs.jspecify)
    }
    testImplementation(testFixtures(project(':')))
    testImplementation(libs.commons.io)
}
